# Руководство по структуре проекта GEMINI

## 📁 Структура папок проекта

```
├── исходные_данные/                 # Папка с исходными данными для расчета
│   └── Для_расчета_коэфф/          # Подпапка с файлами для расчета коэффициентов
│       └── *.xlsx                  # Excel файлы с данными о движении товаров
├── src/                            # Исходный код проекта
│   ├── core/                       # Основной модуль расчетов
│   ├── gui.py                      # Графический интерфейс Flet
│   └── dashboard.py                # Дашборд Streamlit
├── tests/                          # Тесты для проверки работы системы
│   ├── conftest.py                 # Конфигурация pytest и автоматическая очистка
│   └── debugging/                  # Тесты, используемые для отладки
├── docs/                           # Документация проекта
├── examples/                       # Примеры использования системы
├── scripts/                        # Скрипты для отладки и других задач
│   ├── debugging/                  # Отладочные скрипты
│   ├── auto_git_push.py           # Скрипт автоматической отправки в Git
│   ├── setup_auto_git_push.bat    # Скрипт настройки автоматической отправки
│   └── cleanup_test_files.py      # Скрипт автоматической очистки тестовых файлов
├── результаты/                     # Результаты расчетов в формате JSON и HTML
│   ├── архив/                      # Архив старых результатов
│   ├── тестовые_данные/           # Тестовые данные и результаты
│   └── реальные_данные/           # Результаты обработки реальных данных
├── logs/                           # Логи работы системы
│   └── git_auto_push.log          # Логи автоматической отправки в Git
├── документация/                   # Документация по функциональности
├── .venv/                          # Виртуальное окружение Python
├── requirements.txt                # Зависимости проекта
└── pytest.ini                     # Конфигурация тестов
```

## 📊 Работа с исходными данными

### Формат исходных данных

Исходные данные для расчета коэффициентов усушки должны быть в формате Excel (.xlsx) и содержать следующие столбцы:

> **Важно:** Документы инвентаризации содержат информацию о недостачах, выявленных по результатам инвентаризации. Причины недостач могут быть разные - усушка, утеря товара и другие факторы. Система анализирует эту информацию для определения части недостач, связанной с усушкой.

| Столбец                  | Тип данных | Обязательный | Описание                          |
|--------------------------|------------|--------------|-----------------------------------|
| Номенклатура             | Текст      | Да           | Название товара                   |
| Начальный остаток        | Число      | Да           | Начальный баланс товара (кг)      |
| Приход                   | Число      | Да           | Приход товара за период (кг)      |
| Расход                   | Число      | Да           | Расход товара за период (кг)      |
| Конечный остаток         | Число      | Да           | Конечный баланс товара (кг)       |
| Период хранения (дней)   | Целое      | Да           | Длительность хранения в днях      |

### Пример структуры данных:

```
Номенклатура          Начальный остаток  Приход  Расход  Конечный остаток  Период хранения (дней)
ЩУКА СВЕЖАЯ           100.0              0.0     0.0     99.85             7
СКУМБРИЯ С/С          100.0              0.0     0.0     99.91             7
СЕЛЬДЬ Х/К            100.0              0.0     0.0     99.90             7
```

### Размещение файлов

1. Все исходные данные должны размещаться в папке `исходные_данные/Для_расчета_коэфф/`
2. Файлы должны иметь расширение .xlsx
3. Структура столбцов должна соответствовать описанному формату

## 🚀 Запуск расчетов

### 1. Подготовка данных

Создайте Excel файл с данными о движении товаров и поместите его в папку `исходные_данные/Для_расчета_коэфф/`.

### 2. Запуск обработки

Для запуска основного приложения используйте:

> **Примечание:** Система использует принцип FIFO для отслеживания движения партий, но **сами документы инвентаризации не содержат информации о применении FIFO** - эта логика реализована в системе.
```bash
python launch.py
```
Это откроет меню выбора интерфейса (GUI или дашборд).

Для обработки исходных данных напрямую можно использовать скрипты из папки `examples`:
```bash
python examples/обработка_исходных_данных.py
```

### 3. Просмотр результатов

Результаты будут сохранены в папке `результаты/`.

## 🔄 Версионный контроль

### Автоматическая отправка в Git

Система поддерживает автоматическую отправку изменений в удаленный Git репозиторий для обеспечения резервного копирования и контроля версий.

#### Настройка автоматической отправки

Для настройки автоматической отправки изменений в Git каждые 30 минут:

```bash
scripts/setup_auto_git_push.bat
```

Этот скрипт создаст задачу в Планировщике заданий Windows с именем "AutoGitPush", которая будет автоматически отправлять изменения в удаленный репозиторий.

#### Ручной запуск автоматической отправки

Для ручного запуска автоматической отправки изменений в Git:

```bash
python scripts/auto_git_push.py
```

Вы также можете указать собственное сообщение для коммита:

```bash
python scripts/auto_git_push.py "Мое собственное сообщение коммита"
```

#### Логирование

Все операции автоматической отправки в Git записываются в лог-файл:
- `logs/git_auto_push.log` - содержит информацию обо всех операциях отправки

#### Отключение автоматической отправки

Для отключения автоматической отправки:
1. Откройте Планировщик заданий Windows
2. Найдите задачу "AutoGitPush"
3. Удалите задачу или отключите её

## 🧪 Тестирование

Для запуска всех тестов выполните:

```bash
pytest
```
или
```bash
python -m pytest
```

### Автоматическая очистка

После завершения тестов система **автоматически** очищает все созданные файлы:

- Все файлы в директории `результаты/тестовые_данные/`
- Временные файлы (.tmp, .temp, ~, .bak)
- Логи старше 7 дней в директории `logs/`
- Структура проекта остается нетронутой

Автоматическая очистка реализована через плагин pytest, который запускается после завершения всех тестов. Это обеспечивает чистоту проекта и предотвращает накопление временных файлов.

#### Как это работает:
1. При запуске тестов через `pytest` активируется плагин из файла `tests/conftest.py`
2. После завершения всех тестов плагин автоматически вызывает скрипт очистки
3. Скрипт `scripts/cleanup_test_files.py` удаляет все файлы из тестовой директории и очищает старые логи
4. Пользователь получает уведомление о завершении очистки

### Ручная очистка

Если необходимо выполнить очистку вручную:

```bash
python scripts/cleanup_test_files.py
```

### Предпросмотр очистки

Для просмотра файлов, которые будут удалены (без реального удаления):

```bash
python scripts/cleanup_test_files.py --dry-run
```

### Ручная очистка с настройкой дней хранения логов

Для изменения количества дней хранения логов (по умолчанию 7 дней):

```bash
python scripts/cleanup_test_files.py --log-days 3
```

### Ручная очистка без предпросмотра

```
python scripts/cleanup_test_files.py --manual
```

## 📈 Примеры использования

В папке `examples/` находятся скрипты для различных сценариев использования.

## 🛠️ Разработка и отладка

### Добавление новых типов продукции

1. Обновите словарь `product_coefficients` в файле `src/core/adaptive_shrinkage_calculator.py`
2. Добавьте правила определения типа в метод `determine_product_type`
3. Обновите документацию в `docs/ТИПЫ_ПРОДУКЦИИ.md`

### Отладка

Отладочные скрипты следует размещать в папке `scripts/debugging/`.

## 🧹 Организация проекта

### Автоматическая организация

Для автоматической организации структуры проекта используйте скрипт:

```bash
python scripts/organize_project.py
```

Этот скрипт:
- Проверяет структуру директорий
- Создает недостающие директории
- Перемещает файлы в правильные места
- Поддерживает структуру согласно руководству

### Ручная организация

Следуйте этим правилам для поддержания порядка в проекте:

1. **Регулярно запускайте скрипт организации**
2. **Проверяйте структуру перед коммитом**
3. **Удаляйте временные файлы**
4. **Архивируйте старые результаты**

## 📚 Документация

Подробная документация доступна в папке `docs/`.

Дополнительно:
- [Бизнес-логика системы](BUSINESS_LOGIC.md) - Подробное описание принципов работы с данными инвентаризации и расчета усушки
- [Обработка данных инвентаризации](INVENTORY_DATA_PROCESSING.md) - Подробное описание процесса обработки данных инвентаризации
- [Анализ данных инвентаризации](INVENTORY_ANALYSIS_SUMMARY.md) - Сводка по анализу данных инвентаризации и определению усушки
- [Обновления кода ядра](CORE_CODE_UPDATES_SUMMARY.md) - Сводка обновлений кода ядра системы
- [Автоматическая отправка в Git](документация/автоматическая_отправка_в_git.md) - Документация по автоматической отправке изменений в Git

## 📞 Поддержка

При возникновении вопросов или проблем: