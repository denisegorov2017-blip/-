#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Демонстрация обработки неполных данных с отсутствующими инвентаризациями.
"""

import sys
import os
import pandas as pd

# Добавляем путь к папке src для импорта модулей
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '../src')))

from core.shrinkage_system import ShrinkageSystem


def demonstrate_processing_with_missing_data():
    """Демонстрация обработки данных с отсутствующими инвентаризациями."""
    print("🔍 Демонстрация обработки неполных данных")
    print("=" * 60)
    
    # Создаем систему усушки
    system = ShrinkageSystem()
    
    # Пример 1: Данные с отсутствующим начальным остатком
    print("\n📋 Пример 1: Отсутствует начальная инвентаризация")
    missing_initial_data = pd.DataFrame([{
        'Номенклатура': 'Тестовый продукт',
        'Начальный_остаток': None,  # Отсутствует
        'Приход': 1.25,
        'Расход': 0.702,
        'Конечный_остаток': 1.392,
        'Период_хранения_дней': 7
    }])
    
    print("   Обработка данных с отсутствующим начальным остатком...")
    results = system.process_dataset(
        dataset=missing_initial_data,
        source_filename="отсутствующий_начальный_остаток.xlsx",
        use_adaptive=True
    )
    
    if results['status'] == 'success':
        print("   ✅ Обработка завершена успешно")
        print("   💡 Система вывела предупреждение об отсутствии начального остатка")
        print("   💡 Расчет выполнен с имеющимися данными")
    
    # Пример 2: Данные с отсутствующим конечным остатком
    print("\n📋 Пример 2: Отсутствует конечная инвентаризация")
    missing_final_data = pd.DataFrame([{
        'Номенклатура': 'Тестовый продукт',
        'Начальный_остаток': 0.844,
        'Приход': 1.25,
        'Расход': 0.702,
        'Конечный_остаток': None,  # Отсутствует
        'Период_хранения_дней': 7
    }])
    
    print("   Обработка данных с отсутствующим конечным остатком...")
    results = system.process_dataset(
        dataset=missing_final_data,
        source_filename="отсутствующий_конечный_остаток.xlsx",
        use_adaptive=True
    )
    
    if results['status'] == 'success':
        print("   ✅ Обработка завершена успешно")
        print("   💡 Система вывела предупреждение об отсутствии конечного остатка")
        print("   💡 Расчет выполнен с имеющимися данными")
    
    # Пример 3: Полные данные (как в реальных файлах)
    print("\n📋 Пример 3: Полные данные (как в реальных файлах)")
    complete_data = pd.DataFrame([{
        'Номенклатура': 'БЫЧКИ ВЯЛЕНЫЕ',
        'Начальный_остаток': 0.844,  # Присутствует в реальных данных
        'Приход': 1.25,
        'Расход': 0.702,
        'Конечный_остаток': 1.392,
        'Период_хранения_дней': 7
    }])
    
    print("   Обработка полных данных...")
    results = system.process_dataset(
        dataset=complete_data,
        source_filename="полные_данные.xlsx",
        use_adaptive=True
    )
    
    if results['status'] == 'success':
        print("   ✅ Обработка завершена успешно")
        print("   💡 Нет предупреждений об отсутствии инвентаризаций")
        coeff = results['coefficients'][0]
        print(f"   Коэффициенты: a={coeff['a']:.6f}, b={coeff['b']:.6f}, c={coeff['c']:.6f}")
        print(f"   Точность: {coeff['Точность']:.2f}%")
    
    print("\n✅ Демонстрация завершена!")
    print("\n💡 ВАЖНО:")
    print("   • Система работает с теми данными, которые предоставлены")
    print("   • При отсутствии инвентаризаций система выводит предупреждения")
    print("   • Система НЕ запрашивает у пользователя ввод значений, которые уже присутствуют")
    print("   • Начальный остаток должен быть в исходных данных, как в случае с 'БЫЧКИ ВЯЛЕНЫЕ'")


if __name__ == "__main__":
    demonstrate_processing_with_missing_data()