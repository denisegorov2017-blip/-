#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Тест для проверки работы системы предупреждений о наличии инвентаризаций.
"""

import sys
import os
import pandas as pd

# Добавляем путь к папке src для импорта модулей
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '../src')))

from core.shrinkage_system import ShrinkageSystem


def test_inventory_warnings():
    """Тест для проверки работы системы предупреждений о наличии инвентаризаций."""
    print("🔍 Тест системы предупреждений о наличии инвентаризаций")
    print("=" * 60)
    
    # Создаем систему усушки
    system = ShrinkageSystem()
    
    # Тест 1: Полные данные (не должно быть предупреждений)
    print("\n📋 Тест 1: Полные данные")
    complete_data = pd.DataFrame([{
        'Номенклатура': 'БЫЧКИ ВЯЛЕНЫЕ',
        'Начальный_остаток': 0.844,
        'Приход': 1.25,
        'Расход': 0.702,
        'Конечный_остаток': 1.392,
        'Период_хранения_дней': 7
    }])
    
    results = system._check_inventory_availability(complete_data, "полные_данные.xlsx")
    
    # Проверяем, что значения не изменились
    if (results.iloc[0]['Начальный_остаток'] == 0.844 and 
        results.iloc[0]['Конечный_остаток'] == 1.392):
        print("   ✅ Значения остались без изменений (как и должно быть)")
    else:
        print("   ❌ Значения были изменены неправильно")
    
    # Тест 2: Отсутствует начальная инвентаризация
    print("\n📋 Тест 2: Отсутствует начальная инвентаризация")
    no_initial_data = pd.DataFrame([{
        'Номенклатура': 'Тестовый продукт',
        'Начальный_остаток': None,  # Отсутствует
        'Приход': 1.25,
        'Расход': 0.702,
        'Конечный_остаток': 1.392,
        'Период_хранения_дней': 7
    }])
    
    print("   Выполняем проверку инвентаризаций...")
    print("   (В логах должно появиться предупреждение об отсутствии начальной инвентаризации)")
    print("   и автоматическая установка начального остатка равным 0.0")
    
    results = system._check_inventory_availability(no_initial_data, "без_начальной_инвентаризации.xlsx")
    
    # Проверяем, что начальный остаток был автоматически установлен равным 0.0
    if results.iloc[0]['Начальный_остаток'] == 0.0:
        print("   ✅ Начальный остаток автоматически установлен равным 0.0")
    else:
        print(f"   ❌ Начальный остаток не был установлен правильно: {results.iloc[0]['Начальный_остаток']}")
    
    # Тест 3: Отсутствует конечная инвентаризация
    print("\n📋 Тест 3: Отсутствует конечная инвентаризация")
    no_final_data = pd.DataFrame([{
        'Номенклатура': 'Тестовый продукт',
        'Начальный_остаток': 0.844,
        'Приход': 1.25,
        'Расход': 0.702,
        'Конечный_остаток': None,  # Отсутствует
        'Период_хранения_дней': 7
    }])
    
    print("   Выполняем проверку инвентаризаций...")
    print("   (В логах должно появиться предупреждение об отсутствии конечной инвентаризации)")
    
    results = system._check_inventory_availability(no_final_data, "без_конечной_инвентаризации.xlsx")
    
    # Проверяем, что конечный остаток остался None
    if pd.isna(results.iloc[0]['Конечный_остаток']):
        print("   ✅ Конечный остаток остался без изменений (None)")
    else:
        print(f"   ❌ Конечный остаток был изменен неправильно: {results.iloc[0]['Конечный_остаток']}")
    
    # Тест 4: Отсутствуют обе инвентаризации
    print("\n📋 Тест 4: Отсутствуют обе инвентаризации")
    no_both_data = pd.DataFrame([{
        'Номенклатура': 'Тестовый продукт',
        'Начальный_остаток': None,  # Отсутствует
        'Приход': 1.25,
        'Расход': 0.702,
        'Конечный_остаток': None,  # Отсутствует
        'Период_хранения_дней': 7
    }])
    
    print("   Выполняем проверку инвентаризаций...")
    print("   (В логах должны появиться предупреждения об отсутствии обеих инвентаризаций)")
    
    results = system._check_inventory_availability(no_both_data, "без_обеих_инвентаризаций.xlsx")
    
    # Проверяем, что начальный остаток был автоматически установлен равным 0.0
    if results.iloc[0]['Начальный_остаток'] == 0.0:
        print("   ✅ Начальный остаток автоматически установлен равным 0.0")
    else:
        print(f"   ❌ Начальный остаток не был установлен правильно: {results.iloc[0]['Начальный_остаток']}")
    
    print("\n✅ Тест завершен!")
    print("\n📝 ВАЖНО: Проверьте логи выше на наличие предупреждений об отсутствующих инвентаризациях")
    print("   Это часть нормальной работы системы проверки инвентаризаций.")


if __name__ == "__main__":
    test_inventory_warnings()