#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Тест для проверки обработки NaN и пустых значений в системе.
"""

import sys
import os
import pandas as pd
import numpy as np

# Добавляем путь к папке src для импорта модулей
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '../src')))

from core.shrinkage_system import ShrinkageSystem


def test_nan_handling():
    """Тест для проверки обработки NaN и пустых значений."""
    print("🔍 Тест обработки NaN и пустых значений")
    print("=" * 50)
    
    # Создаем систему усушки
    system = ShrinkageSystem()
    
    # Создаем тестовые данные с различными типами пустых значений
    test_data = pd.DataFrame([
        {
            'Номенклатура': 'Тестовый продукт 1',
            'Начальный_остаток': np.nan,  # NaN значение
            'Приход': 2.0,
            'Расход': 1.5,
            'Конечный_остаток': 0.5,
            'Период_хранения_дней': 7
        },
        {
            'Номенклатура': 'Тестовый продукт 2',
            'Начальный_остаток': None,  # None значение
            'Приход': 3.0,
            'Расход': 2.0,
            'Конечный_остаток': 1.0,
            'Период_хранения_дней': 7
        },
        {
            'Номенклатура': 'Тестовый продукт 3',
            'Начальный_остаток': '',  # Пустая строка
            'Приход': 1.0,
            'Расход': 0.5,
            'Конечный_остаток': 0.5,
            'Период_хранения_дней': 7
        },
        {
            'Номенклатура': 'Тестовый продукт 4',
            'Начальный_остаток': 0.0,  # Ноль
            'Приход': 4.0,
            'Расход': 3.0,
            'Конечный_остаток': 1.0,
            'Период_хранения_дней': 7
        }
    ])
    
    print("📋 Исходные данные:")
    print(test_data.to_string(index=False))
    
    print("\n📋 Выполняем проверку инвентаризаций...")
    
    # Выполняем проверку
    results = system._check_inventory_availability(test_data, "тестовый_файл.xlsx")
    
    print("\n📋 Результаты после проверки:")
    print(results.to_string(index=False))
    
    # Проверяем, что все пустые значения установлены в 0.0
    success = True
    for idx, row in results.iterrows():
        if row['Начальный_остаток'] != 0.0 and pd.isna(row['Начальный_остаток']):
            print(f"❌ Найдено необработанное пустое значение для {row['Номенклатура']}")
            success = False
    
    if success:
        print("\n✅ Все пустые значения успешно обработаны и установлены в 0.0")
    else:
        print("\n❌ Найдены необработанные пустые значения")


if __name__ == "__main__":
    test_nan_handling()