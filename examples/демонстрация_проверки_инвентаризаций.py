#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Демонстрация работы системы проверки наличия инвентаризаций.
"""

import sys
import os
import pandas as pd

# Добавляем путь к папке src для импорта модулей
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '../src')))

from core.shrinkage_system import ShrinkageSystem


def demonstrate_inventory_check():
    """Демонстрация работы системы проверки инвентаризаций."""
    print("🔍 Демонстрация системы проверки наличия инвентаризаций")
    print("=" * 60)
    
    # Пример 1: Полные данные (как у "БЫЧКИ ВЯЛЕНЫЕ")
    print("\n📋 Пример 1: Полные данные (как у 'БЫЧКИ ВЯЛЕНЫЕ')")
    complete_data = pd.DataFrame([{
        'Номенклатура': 'БЫЧКИ ВЯЛЕНЫЕ',
        'Начальный_остаток': 0.844,
        'Приход': 1.25,
        'Расход': 0.702,
        'Конечный_остаток': 1.392,
        'Период_хранения_дней': 7
    }])
    
    system = ShrinkageSystem()
    print("   Обработка данных без предупреждений...")
    results = system.process_dataset(
        dataset=complete_data,
        source_filename="полные_данные.xlsx",
        use_adaptive=True
    )
    
    if results['status'] == 'success':
        print("   ✅ Обработка завершена успешно")
        coeff = results['coefficients'][0]
        print(f"   Коэффициенты: a={coeff['a']:.6f}, b={coeff['b']:.6f}, c={coeff['c']:.6f}")
        print(f"   Точность: {coeff['Точность']:.2f}%")
    
    # Пример 2: Отсутствует начальная инвентаризация
    print("\n📋 Пример 2: Отсутствует начальная инвентаризация")
    no_initial_data = pd.DataFrame([{
        'Номенклатура': 'Тестовый продукт',
        'Начальный_остаток': None,  # Отсутствует
        'Приход': 1.25,
        'Расход': 0.702,
        'Конечный_остаток': 1.392,
        'Период_хранения_дней': 7
    }])
    
    print("   Обработка данных с отсутствующей начальной инвентаризацией...")
    results = system.process_dataset(
        dataset=no_initial_data,
        source_filename="без_начальной_инвентаризации.xlsx",
        use_adaptive=True
    )
    
    # Пример 3: Отсутствует конечная инвентаризация
    print("\n📋 Пример 3: Отсутствует конечная инвентаризация")
    no_final_data = pd.DataFrame([{
        'Номенклатура': 'Тестовый продукт',
        'Начальный_остаток': 0.844,
        'Приход': 1.25,
        'Расход': 0.702,
        'Конечный_остаток': None,  # Отсутствует
        'Период_хранения_дней': 7
    }])
    
    print("   Обработка данных с отсутствующей конечной инвентаризацией...")
    results = system.process_dataset(
        dataset=no_final_data,
        source_filename="без_конечной_инвентаризации.xlsx",
        use_adaptive=True
    )
    
    # Пример 4: Отсутствуют обе инвентаризации
    print("\n📋 Пример 4: Отсутствуют обе инвентаризации")
    no_both_data = pd.DataFrame([{
        'Номенклатура': 'Тестовый продукт',
        'Начальный_остаток': None,  # Отсутствует
        'Приход': 1.25,
        'Расход': 0.702,
        'Конечный_остаток': None,  # Отсутствует
        'Период_хранения_дней': 7
    }])
    
    print("   Обработка данных с отсутствующими обеими инвентаризациями...")
    results = system.process_dataset(
        dataset=no_both_data,
        source_filename="без_обеих_инвентаризаций.xlsx",
        use_adaptive=True
    )
    
    print("\n✅ Демонстрация завершена!")
    print("\n💡 ВАЖНО: При отсутствии инвентаризаций система выводит предупреждения в лог,")
    print("   что помогает выявить потенциально неточные расчеты коэффициентов усушки.")
    print("   Система работает с теми данными, которые предоставлены, и не запрашивает")
    print("   у пользователя ввод значений, которые уже присутствуют в данных.")


if __name__ == "__main__":
    demonstrate_inventory_check()